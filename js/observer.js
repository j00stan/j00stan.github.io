const faders=document.querySelectorAll(".fade"),appearOptions={threshold:.125},appearOnScroll=new IntersectionObserver(function(e,r){e.forEach(e=>{e.isIntersecting&&(e.target.classList.add("appear"),r.unobserve(e.target))})},appearOptions);faders.forEach(e=>{appearOnScroll.observe(e)});
